# --- MQL5 build artifacts (never commit) ---------------------------------
*.ex5
*.ex4
*.log
compile.log
otto_build/

# --- local pristine snapshots kept under _tools --------------------------
_tools/_pristine_*/

# --- transient tool scratch ----------------------------------------------
_tools/commit_msg.txt
_tools/commit_msg_*.txt

# --- OS / editor cruft ---------------------------------------------------
Thumbs.db
desktop.ini
.vscode/
*.tmp
_tools/__pycache__/
*.pyc

# --- redundant local checkouts of this same repo -------------------------
# OTTO-v5.00/ is a separate clone of github.com/vickygujjar17/OTTO-v5.00.git
# kept inside this working tree (at v5.27). It has its own .git, so adding it
# would record a broken "gitlink" (phantom submodule) instead of its files.
# Ignore it; delete the folder once it is no longer needed.
OTTO-v5.00/